# Market data provider: "yahoo" or "demo"
MARKET_PROVIDER=demo

# Database path (relative to backend/)
DB_PATH=../signallens.db

# Server
HOST=0.0.0.0
PORT=8000

# CORS origin for frontend (if served separately)
CORS_ORIGIN=http://localhost:8000

# Logging level
LOG_LEVEL=INFO
